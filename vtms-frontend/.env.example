# All environment variables are defined in the repo root:
#   ../.env.example  (template)
#   ../.env.local    (your local values — git-ignored)
#
# Copy ../.env.example → ../.env.local and fill in.
# Mirror the same values in Vercel → Settings → Environment Variables.
