VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only secret (used by the future admin-invite serverless function).
# Do NOT prefix with VITE_ — Vite exposes VITE_-prefixed vars to the client bundle.
# Set this in Vercel's dashboard env vars for production; .env files are not deployed.
SUPABASE_SERVICE_ROLE_KEY=
