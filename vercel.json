{
  "buildCommand": "cd vtms-frontend && npm install && npm run build",
  "outputDirectory": "vtms-frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api?__path=$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
